[project]
name = "py-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4.0",
]

[tool.setuptools.packages.find]
include = ["py_template*"]
exclude = []

[tool.pytest.ini_options]
testpaths = ["test_py_template"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
markers = []
